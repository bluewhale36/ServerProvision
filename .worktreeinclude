# git worktree 생성 시 새 워크트리로 복사할 비추적(gitignore) 파일 목록.
# claude --worktree 로 만든 워크트리에 자동 복사되어, 세션마다 로컬 설정을 다시
# 만들 필요가 없게 한다. 비밀값이 든 파일은 커밋되지 않으면서도 승계된다.

# 빌드·실행 명령과 로컬 개발 DB 접속 환경변수(SERVER_PORT/DB_URL/DB_USERNAME/DB_PASSWORD).
CLAUDE.local.md
