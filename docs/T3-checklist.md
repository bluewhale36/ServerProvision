# T3 체크리스트 — 물리 서버(실기) 확보 시 몰아서 확인할 항목

> **운영 규약** (유스케이스 토론 Q-C · DEC-35, 2026-07-19 확정): 물리 서버 없이는 검증할 수 없는(T3)
> 항목을 슬라이스가 만들 때마다 여기 한 줄씩 적립한다. 물리 서버가 확보되면 이 목록을 위에서부터
> 소화하고, 완료 항목은 체크 + 결과 한 줄을 남긴다. Notion 후속 마일스톤 기재 규약(DEC-9)과 병행.

## 적립 목록

### E1 — 진단 리눅스
- [ ] **V3** (E1-R): `modprobe ipmi_devintf ipmi_si` 후 `/dev/ipmi0` 생성 여부 + `ipmitool lan print` 유효 채널 번호(1 이 아닐 수 있음) — 4종 보드 각각. E1-2 수집 스크립트의 전제.
- [ ] **V8** (E1-R): `dmidecode -s system-uuid` / `-s baseboard-serial-number` 가 placeholder("To Be Filled By O.E.M." 류)가 아닌지 — 4종 보드 각각. E1-2 수집 파서의 placeholder 필터 설계 입력.
- [ ] **실 iPXE 펌웨어 거동** (E1-1): 실보드 NIC 의 PXE ROM 이 `chain http://` · `||`/`goto` · 커널 인자 `initrd=` 를 처리하는지 (QEMU ROM 과 다를 수 있음 — V7 의 실기판). 참고 실측(2026-07-19 T2): QEMU legacy BIOS iPXE ROM 은 스크립트 부트파일 실행 OK / OVMF(UEFI) 네이티브 PXE 는 텍스트 스크립트를 실행 못 함 — 실기 UEFI 는 dhcpd→tftp `ipxe.efi` 2단 체인이라 구조가 다르며(E1-I 소관) 이 항목에서 실보드로 확인한다.
- [ ] **실보드 NIC 드라이버** (E1-1): modloop-lts 의 모듈셋으로 실 NIC 이 잡히는지 (진단 리눅스에서 `ip link` 확인).
- [ ] **PCIe 카드 lspci 실측** (E1-2): 사내 사용 카드(RAID·10G UTP/SFP+·FC 16/32Gb·NVIDIA GPU)의 lspci 출력 수집 — 종류 분류 규칙(kind) 보강 입력. 4종 보드 + 대표 카드 조합. **`lspci -nn` 으로 뜰 것**(U4-1 토론 8회차) — 기본 출력은 Vendor/Device(=칩)만 이름으로 바꾸므로 리브랜드 카드의 상품명이 보이지 않는다. 카드 식별에 필요한 것은 `Subsystem` 줄의 `[벤더:디바이스]` id 다.
- [ ] **dmidecode 메모리 슬롯 표기 실측** (E1-2): 실보드의 DIMM Locator 문자열 형식 확인 — 슬롯 표시 UI 정합 입력.

### E2 — 펌웨어 (슬라이스 진행 시 구체화)
- [x] ~~**flash 집행** (DEC-20): 가상 USB 이미지 부팅 → BIOS flash 실행~~ **→ 2026-08-20 무효화**: 집행 경로가 Redfish API 로 최종 확정(가상 USB 철폐)되어 이 항목 자체가 소멸. Redfish 실집행은 E0-4 실측(BIOS 47초~2분22초 · BMC 7분37초)으로 기검증.
- [ ] Redfish SimpleUpdate 로 커스텀 BIOS 파일이 적용되는지 (E3-R 조사의 실기 확인 항목). **→ 2026-08-19 부분 실측(원장 = Notion E0-4)**: 표준 `image.RBU` 는 `UpdateComponent: "BIOS"` 로 동일 버전(F27, 47초) · 상위 버전(F29, 2분 22초) 모두 flash 성공 — 단 **1영역(`BIOS`)만 갱신되고 `BIOS2` 는 버전 미노출로 거동 불명**. **로고 변경본(커스텀 = GIGABYTE 에 요청해 제공받는 boot 로고 변경 이미지)을 RBU 경로로 flash 하면 boot 로고만 반영되지 않음 — 그 외 갱신은 정상**(2026-08-19 정정: 종전 "기본 이미지로 flash 됨" 은 과대 서술이었다). 벤더 문의(로고 변경본의 RBU 형식 제공 여부 · 로고 미반영 원인) 후 방향 재조정, 이 항목은 그때까지 미결 유지. **→ 2026-08-19 L1 실측(4호 §8): 보유 "로고 변경본 RBU" 가 정본 image.RBU 와 SHA-256 동일 — 로고 미반영의 원인 확정(로고가 들어 있지 않은 파일을 flash 한 것, RBU 경로 결함 아님). 미반영 원인 문의는 불요화, 남은 게이트 = 로고 변경본의 RBU 형식 제공 요청(벤더 메일 발송 예정).** **→ 2026-08-20 종결: 벤더로부터 로고 변경본 RBU 수령, 정본과 SHA-256 상이 확인(로고 포함 실재) → 가상 USB 철폐 · BIOS · BMC 펌웨어 업데이트 = Redfish API 최종 확정. 이행 = R12(bundle/zip 업로드 제거 + flash 파일 확장자 검사, M 스트림). 로고 반영의 실flash 확인은 수령본 첫 사용 시 자연 검증.** 2영역 갱신은 `UpdateComponent` 허용값의 `HPM_BIOS2` 가 후보(미실측).
- [x] ~~fat32-lib 생성 이미지의 sanboot 전달 검증~~ **→ 2026-08-20 무효화**: 가상 USB 철폐(Redfish 확정)로 소멸. J1 실측(fat32-lib 이미지 실기 부팅 통과)과 probe 이미지는 기록 자산으로만 잔존.

#### E2-3 착수 게이트 (2026-08-08 신설 — BMC 펌웨어 13.06.27 이 UEFI Shell 경로를 폐쇄)
근거: `discussion/26-08-08_14-48-20_E2-bmc-redfish-pivot_discussion.md`. BMC 집행이 가상 USB 에서 Redfish SimpleUpdate 로 전환되면서 착수 게이트가 "이미지 포맷·하부 도구 확정" 에서 아래 셋으로 교체됐다.

- [x] **13.06.27 에서 UpdateService OEM 계약이 유지되는가** — `GET /redfish/v1/UpdateService` 로 `Oem.AMIUpdateService`(FlashPercentage · UpdateStatus · UpdateTarget)와 `Actions/SimpleUpdate` URI 존재 확인. E3-R 조사가 "최신 펌웨어에서 유지되는지 확인 불가" 로 남긴 항목이며, 13.06.27 이 정확히 그 대상이다. **→ 2026-08-18 실측 완료(원장 = Notion E0-4)**: 유지 확인 — 단 계약 모양이 갱신됐다. 액션 명명은 표준 `#UpdateService.SimpleUpdate`(target 경로는 조사값과 동일), 진행 필드는 `Oem.AMIUpdateService.UpdateInformation.{FlashPercentage · UpdateStatus · UpdateTarget}` 한 겹 안으로, `PreserveConfiguration` 은 `{"BMC": true}` 단순형으로 바뀌었다. `MultipartHttpPushUri` = `/redfish/v1/UpdateService/upload` 유지, RedfishVersion 1.15.1. **E2-3 구현의 계약은 조사값이 아니라 이 실측 스키마를 따른다.**
- [x] **게스트 전원 OFF 상태에서 SimpleUpdate 가 수락되는가** — BMC 업데이트는 게스트 전원이 꺼진 상태에서 진행해야 한다(전원 선은 연결 유지, BMC 는 대기 전력으로 생존). 공식 가이드는 "업데이트 중 BMC WebGUI 접속 금지" 만 명시하고 전원 상태 요건은 미기재라 실측이 필요하다. **→ 2026-08-18 실측(원장 = Notion E0-4)**: SimpleUpdate 실집행(13.06.27 `.ima_enc`, HTTP pull)이 다운로드 → 검증 → flash → 완료(Task OK, 17:20:41~17:28:18 약 7분 37초)로 완주했고, 같은 세션에서 `PowerState: "Off"` 가 실측됐다. 완료 후 BMC 재시작으로 잠시 접속이 거부되다 동일 IP · credential증명으로 복귀한다.
- [ ] **BMC 업데이트 실패 후 재시도 경로** — 듀얼 이미지(`DualImageConfigurations`)로 벽돌 위험은 낮으나 복구 절차 확인 필요. HPE Cray CSM 선례: `ipmitool mc reset cold` 후 5 분 뒤 재시도.

### U4 — 디스크 · RAID (세팅 정의서 확장, 2026-08-17 신설)

근거: `discussion/26-08-15_22-12-47_U4-1-disk-features_discussion.md` ~ `26-08-17_19-40-39_…-8_discussion.md` (8회차).
U4-1 은 정의서가 **RAID 구성과 디스크 역할을 적는 것**까지만 하고 집행은 E 영역에 남긴다. 아래는 그 경계에서 실물 없이 확정할 수 없어 남긴 것들이다.

- [ ] **주력 RAID 카드 둘의 Subsystem ID 실측** (U4-1-1 / MA-raidcard): `lspci -nn` 으로 **GIGABYTE CRA3338**(캐시 없음)과 **AVAGO 9361-8i**(캐시 2GB)의 `Subsystem: … [벤더:디바이스]` 값을 각각 수집. 리브랜드 카드는 Vendor/Device 가 원 칩 제조사(Broadcom `0x1000`) 것이라 **기본 lspci 로는 두 카드가 같아 보일 수 있다** — 자원 등록의 '정밀 등록'(관측 채움)과 정의서의 자동 탐지(AUTO)가 이 값에 걸려 있다. **→ 2026-09-03 E3.5-5-b 구현**: 손으로 옮겨 적는 대신 카드 관리 화면이 "이 카드를 정의서로 지정한 게스트" 의 진단 관측(E3.5-5-a 봉투)을 보여 주고 [관측값으로 확정] 으로 채운다. **실기 확인 항목**: ① 실 게스트 진단 후 정의서 할당 → 카드 화면에 관측 배지 · 서버 링크가 뜨는가 ② 확정 후 게스트 상세 예고가 "일치" 로 바뀌는가 ③ 두 카드(CRA3338 · 9361-8i)의 관측값이 실제로 다른가(같으면 리브랜드 함정 — 대조가 무의미해진다).
- [ ] **CRA3338 의 PCI 클래스 표기 확인** (U4-1-1): BMC inventory 에서 `Serial Attached SCSI controller` 로 잡힌다는 것이 확인됐다(E0-3 PCI 섹션). **lspci 에서도 같은 클래스로 뜨는지** 확인. 지금 파서 `DiagnosticReportParser.kindOf` 는 `raid` · `megaraid` 문자열이 있을 때만 `RAID` 로 분류하므로 **이 카드가 `ETC` 로 떨어진다** — 실측 문자열을 받아 분류 규칙을 보강해야 한다.
- [ ] **두 카드의 RAID 구성 명령이 실제로 다른가** (E 계열): 사용자는 다르다고 알고 있으나 정확한 명령은 미상. **E 슬라이스에서 WebSearch 로 선행 조사**한 뒤 실기로 확인한다. 참고 — 같은 MegaRAID 칩 계열이면 `storcli` 로 함께 다뤄질 가능성이 있고, 그렇다면 명령을 가르는 것은 캐시 유무가 아니라 **칩 계열과 동작 모드(RAID / IT-HBA)** 다.
- [ ] **CRA3338 의 RAID 레벨 한계 실기 확인** (U4-1-1): 사용자 확인으로 **RAID0 · RAID1 만 지원**(최소 2개 디스크), RAID5 이상 불가. 이것이 업무 관례("같은 스펙 3개 이상이면 캐시 있는 카드")의 **진짜 이유**다 — 캐시가 아니라 RAID5 를 만들 수 있는 컨트롤러인가가 가른다. 실기에서 그 한계를 확인하고, 자원의 '지원 RAID 레벨' 필드 설계 입력으로 쓴다.
- [ ] **디스크 장착 순서 실측** (U4-1-2): 업무 관례상 **SSD → HDD, 용량 작은 것 → 큰 것** 순으로 장착된다. 이 순서가 `lsblk`/`lspci` 수집 결과의 나열 순서와 일치하는지 — 볼륨 우선순위(OS 영역 자동 선택)가 이 순서를 전제한다.

### E3 — BIOS/BMC 설정 (슬라이스 진행 시 구체화)
- [x] **실 BMC Redfish**: `/redfish/v1` 버전 · `Systems/Self/Bios/SD` 실재 · 계정 PATCH · 기본 비밀번호(시리얼 끝 11자) 정책 — E3-R 체크리스트 8항목. **→ 2026-08-19 3호 실측으로 사실상 완결(원장 = Notion E0-4-3)**: `PATCH /redfish/v1/Systems/Self/Bios/SD` 204(`If-Match: *`) → pending 생성("Future BIOS Settings") → ForceRestart 적용 → 원복까지 **전 왕복 실증**. 2호의 GET 404 는 "빈 pending 은 GET 404, PATCH 는 수락" 거동으로 확정 — E3-1 전제 완전 복구. `BiosAttributeRegistry.json` 273속성(Enumeration 256 · Integer 15 · Password 2, DefaultValue · 허용값 · ResetRequired · Dependencies 47) 채집 = E3-1 · biossetting 검증 룰 원천. 세션 = `SessionTimeout` 30초 · 상한 10개 실측(발급 · 토큰 사용 정상, E1.5 는 Basic 우선 + Task 추적 등 단기 세션 병용). 잔여 1: 공장 기본 비밀번호 정책(신품 개봉 시 확인).
- [x] **E3-2 쓰기 표면 실측(5호 M 계열, 2026-08-20 — 원장 = Notion `E0-4-5`)**: ① `Managers/Self` 에 `@Redfish.Settings`(pending) 부재 → **BMC 설정 쓰기 모델 = 즉시 PATCH**(ETag `If-Match`) — BIOS 의 pending + 재부팅 모델과 비대칭, 재부팅 오케스트레이션 불요. ② **이중 클라이언트 확정**: Syslog 는 AMI 웹 API — `PUT /api/settings/log`(server_addr · port 514 · remote/local 플래그) 쓰기 계약을 HAR 로 확보(3호 이월 H4 해소). 인증 = `POST /api/session`(form) + 세션 쿠키. 시간 · NTP 는 양쪽 표면 존재(Redfish `NetworkProtocol.NTP` + 웹 `PUT /api/settings/date-time`) — Redfish 우선 원칙 제안. ③ 네트워크(EthernetInterfaces — Static · VLAN · DNS) · SNMP · 계정(POST Accounts, Min/MaxPasswordLength 6/20, lockout 정책) 전부 Redfish 표면 실재. ④ **보너스: `AMIManager.BackupConfig`/`RestoreConfig` Oem 액션 실재** — 개별 PATCH 대신 골든 설정 복원이라는 E3-2 대안 전략 후보. GCT(GIGABYTE) Oem 트리(Firewall · FrontPanelSettings)도 실재. **관찰**: BMC 시계가 2026-08-03 로 틀어짐(NTP ProtocolEnabled=false — 시간 설정이 실무 필수임을 실증). N1~N3 경로 · param · 인증 계약은 HAR 에서 확정해 실측 레시피로 이관(`discussion/26-08-20_17-45-50_bmc-redfish-fieldwork-5-N-recipe.md` — Redfish=Basic+If-Match, AMI 웹=POST /api/session 응답 CSRFToken 을 X-CSRFTOKEN 헤더로 되돌림). **→ 2026-08-25 정정**: N1~N3(Redfish NTP · eth0 · syslog)은 사용자 확정 없이 상정한 후보였고 표준 항목이 아니다 — 폐기. **사내 표준 BMC 세팅의 정본 = Notion `E0-3`(AMI 웹 API 4종 — DateTime · Cold Redundant · Fan Profile · Network Bond, 바디 채집 완료)**. E3-2 계약은 E0-3 이 정본이며 syslog · SNMP · SMTP 는 대상이 아니다. 잔여 = **W1~W5**(E0-3 4종의 브라우저 밖 curl 재연 — 세션 · CSRF · Bond 후 세션 삭제 · 지속성, 레시피 정정판) + M5.

### 강화 확장 (DEC-35 — E3 이후, 전원 제어 3종)
- [x] **Redfish 전원 제어**: ComputerSystem.Reset(On/ForceOff/GracefulRestart) 실측 — UC-2 즉시 강제 정지 · phase 전환 재부팅 신뢰성의 전제. **→ 2026-08-19 완결(원장 = Notion E0-4)**: On 재투입 성공, GracefulShutdown 정상 동작, `ResetActionInfo` 허용값 5종 실측(`ForceOff` · `ForceRestart` · `GracefulShutdown` · `On` · `PowerCycle` — 조사값의 GracefulRestart 는 부재). **실패 모드 1종 실측**: On 이 "IPMI 명령은 성공했으나 전원 상태 불변" Exception(Critical)으로 떨어진 사례 1회 — 수동 cold boot 개입 후 재시도로 성공. E1.5 클라이언트는 On 발행 후 `PowerState` 폴링 검증 + 실패 시 `PowerCycle` 폴백 + 운영자 알림을 설계에 포함해야 한다. **(2026-08-08 이관 — 이 항목은 신설된 `E1.5 : Redfish 제어 기반 · 전원 제어` 소관이며, `E2-3` 의 착수 게이트다.** BMC 업데이트가 `BIOS flash → ForceOff → BMC flash → On → 검증` 흐름을 요구하므로 전원 제어 없이는 시작도 종료도 못 한다. 아래 나머지 2 종은 E1.5 범위 밖 — 필요해지는 시점에 그 클라이언트 위에 얹는다.)
- [ ] **BootSourceOverride**: 다음 1회 부팅을 PXE 로 강제 — UC-4(network boot 이탈) 원격 복구의 전제. **→ 2026-08-19 승격 검토(4호 세션 관찰)**: OS 설치 서버의 boot priority 가 OS 1순위 · USB/네트워크 후순위임이 관찰돼 "프로비저닝 게스트는 상시 PXE 우선" 전제가 실기에서 깨짐. 재프로비저닝 대상(기존 OS 보유)과 phase 전환 재부팅(특히 flash 의 설정 리셋과 결합 — 수동 PXE 우선 설정이 flash 한 번에 무효)에 대비해 UC-4 복구 전용이 아니라 **프로비저닝 흐름 필수 배선(E1.5 합류 후보)** 으로 승격 검토. Once/Continuous 선택 · `Systems/Self/SD`(ComputerSystem pending) 경유 여부는 4호 K1 실측 입력. **→ 2026-08-27 실기 확정**: 통합 테스트에서 OS 가 남은 서버(`아이티인포 (하)`, MD72-HB3)가 BMC flash 후 전원 투입 뒤 디스크의 OS 로 부팅해 20분 복귀 시한을 넘겼다(첫 PXE DHCP 요청은 전원 투입 35분 뒤 수동 재시작 시점). 실측 부트 순서 `FBO101='Hard Disk'` 1순위. **사용자 결정 — network boot 강제는 별도 슬라이스**로 진행(전원 투입 · 재부팅 경로 = `RedfishPowerService`, E2 · E3 공용). 남은 실측: AMI 의 `BootSourceOverrideEnabled=Once` + `Target=Pxe` PATCH 수락 · 적용 경로(`Systems/Self` 직접 vs `Systems/Self/SD` pending) · flash 후 설정 리셋과의 순서. **→ 2026-08-31 E2.5 배선(CP4)**: 엔진의 전원 투입(`PowerOnStep`) · 재부팅(`BeginSettingStep`) 직전에 `NextBoot.PXE_ONCE` 무장(`PATCH Systems/Self` Boot{Once · Pxe · UEFI} + If-Match 사다리 `*` → 412 시 fresh ETag) 후 되읽기 관찰(APPLIED / UNCONFIRMED / REJECTED)을 결과 메시지 · 로그에 남긴다 — 거절은 best effort(전원 명령 계속). 남은 실측(K1-b 수락 · PXE 진입 · Once 소진 / K1-d 직접 vs `SD` pending · If-Match 요건 / Mode UEFI 명시 필요 여부 / BMC flash 뒤 boot flag 잔존)은 CP7 실기에서 이 로그의 outcome 으로 판정한다. **→ 2026-09-16 HF17(실측 5건 · plan/26-09-16_15-39-11_HF17_plan.html)**: HF15-1 의 `Continuous` 가 AMI BIOS 의 persistent device 기억을 만들어 부트 순서를 매 POST 덧씌우고(network ↔ disk 자리 교체 · 셋업 저장 무효 · CMOS 클리어만 복구) Redfish `Disabled` · `Once` · `Continuous · None` 으로는 지워지지 않음이 확정됐다. 정정 = Continuous 전면 폐기 · Once 회귀 · REBOOT 지시 직전 서버 무장 · 재부팅 뒤 4분 미도착 시 Once 재무장 1회(설정 · 펌웨어 step). **CP7 실기 확인 항목**: K-A 진단 → RAID → OS 전이(에이전트 reboot)가 PXE 로 복귀 · K-B BIOS 설정 재시작 뒤 4분 안에 안 오면 재무장 1회로 PXE 도착(로그 `PXE 미도착 → Once 재무장`) · K-C 종단 뒤 **BIOS 셋업 부트 순서가 disk 1번 그대로이고 셋업 편집이 유지됨**(Redfish `Disabled` 만 보던 실기 4호 K5 를 대체) · K-D `bootPatches`/BMC 로그에 Continuous 0회. 영향 서버(09-09 이후 창을 지난 전부)의 복구 = `Continuous · Hdd · UEFI` PATCH → 재부팅 → `Disabled`(순서 복구 · 기억은 Hdd 세미핀) / 완전 정리 = CMOS 클리어 또는 `Bios.ResetBios` + E3-1 설정 재적용.
- [x] **proxyDHCP 첫 접촉(R16 · 사내망 실기)** — 2026-09-17 통과(사내망 10.1.1.0/24 · AMI UEFI ROM arch 7 → ipxe.efi → boot.ipxe → /boot → 진단 · 굽기 · 설정 · RAID · Windows 완주. 정정 3 = proxy 모드 iPXE 단도 pxe-service · CSA 두 이름 · 방화벽 4011/udp. 잔여 = 다중 NIC listen-address · 자체 DHCP 모드 Linux 실기): 실 PXE ROM(AMI UEFI)이 dnsmasq proxy 의 `pxe-service` 응답을 받아 `ipxe.efi` 를 받는가 · iPXE 둘째 단이 `dhcp-boot=tag:ipxe` 로 `boot.ipxe` 를 받는가 · `dnsmasq --test` 비특권 통과 · 재기동 20초 안 · 다중 NIC 에서 `listen-address` NIC 만 응답 · SELinux enforcing 에서 조각 · 임대 파일 읽기. 실기는 사내망(spv-01)에서 진행하고 서브넷 · 부트 서버 IP 는 그때 지정한다(2026-09-16 사용자 결정). 실패하면 스위치 DHCP snooping · VLAN 을 먼저 본다. 예비안 = `ipxe.efi` 임베디드 스크립트 빌드.
- [ ] **IndicatorLED (UID 램프)**: 상세 페이지 버튼 → 실물 램프 점멸 — UC-5 식별 후보 4.

### HF20 — 부트 순서 정착 (2026-09-18 신설)
- [ ] **첫 재진입 시점의 DISK_FIRST (G-1b)** (HF20 F-2): Once=Hdd 로 디스크 부팅한 다음 POST 에서 BIOS 가 오버라이드 직전의 그룹 순서를 복원해, Windows Setup 의 두 번째 재부팅이 PXE 를 한 번 거쳐(재진입 1회 · `exit` 폴스루) 디스크로 간다(K-3 20:31~20:32). `WindowsInstallingExecutor.onBootScriptServed` 의 재진입 갈래에서 `settleBootOrder(DISK_FIRST)` 를 적용하면 그 우회가 사라지는지 — 비용 약 2 분 · 재진입 상한 5 회에 여유가 있어 별도 슬라이스로.
- [ ] **OS 없는 서버의 종단 뒤 부트 순서** (HF20 F-3): 디스크 부팅 항목이 없는 서버에서 굽기 · 설정만 하는 정의서를 끝내면 `DISK_FIRST` 가 무동작이라 마지막 Once POST 의 NET 1순위가 남는다(실해 없음). 또 OS 단계 없는 종단 게스트는 `awaitingIntake` 30 초 폴링에 상주해 스스로 재부팅하지 않으므로(DEC-25), 이미 OS 가 있는 서버에 굽기 · 설정만 돌리는 운영에서는 운영자가 재부팅해야 OS 로 간다(K-1 19:00~19:08). 운영 정책으로 둘지 종단 시 재부팅 지시를 낼지 판단.
- [ ] **RAID phase 진입 직후 첫 보고의 409** (HF20 F-4): 게스트가 RAID phase 로 부팅한 뒤 첫 `POST /api/pxe/v1/agent/steps` 가 `AgentReportRejectedException` 409 로 거절되고 12 초 뒤 정상 201 로 이어졌다(K-2 19:43:15 · 진행 영향 없음). 사유(진단 step 잔류 보고인지 · 커서 불일치인지)를 로그 · 에이전트 원문으로 확인.
- [ ] **모의 게스트 하네스에 정착 시나리오** (HF20): `scripts/mock-guest/` 에 무장 직전 SHELL_LAST · 종단 DISK_FIRST · Windows 착수 Once=Hdd 를 모의 BMC(`bootOrderPatches` · `bootedVia`)로 재연하는 시나리오 추가.
- [ ] **통신 다이어그램 반영** (HF20 · 다이어그램 규약): `diagram/guest-provisioning-canvas.html` 에 BMC 통로의 BootOrder PATCH 두 지점과 Windows 착수 Once=Hdd 를 추가.
- [ ] **정의서 9 의 18차 재실행** (HF20 K-2 재검): K-2 는 17차에서 셸 낙하(G-1) 뒤 수동 개입으로 완주했다. 굽기 + 설정 + RAID + Windows 전 경로를 18차(G-1 보완)로 손대지 않고 완주하는지 한 번 더.

### S19 — 게스트 채널 인증 (2026-09-15 신설)
- [ ] **게스트 토큰 인증의 실기 무회귀** (S19-1): 실보드의 진단 에이전트(`X-Guest-Token`) · Windows 완료 보고 · 토큰 번들 서빙 · 펌웨어 ImageURI 가 전용 게스트 체인(401 JSON 거절)에서 종전과 같이 통과하는지. 위조 · 회수 토큰이 404 가 아니라 401 인지(하네스는 401 로 갱신).
- [ ] **실 iPXE 의 userinfo URL 처리** (S19-2 D-4): 실보드 펌웨어 내장 iPXE(UEFI · 벤더 빌드)가 tftp `boot.ipxe` 의 `chain http://pxe:<secret>@<서버>/api/pxe/v1/boot?…` 에서 Basic credential 을 붙여 보내는지 — QEMU 내장 iPXE ROM 은 랩(CP5)에서 확인했고, 벤더 빌드는 기능 집합이 다를 수 있다. 실패하면 콘솔 "Permission denied" 로 멈추며 앱에는 401 만 남는다(등록 없음).
- [ ] **커널 · initramfs · modloop · apkovl · apk 저장소의 credential URL 수신** (S19-2 D-4): 실보드에서 iPXE(`kernel` · `initrd`)와 Alpine initramfs 의 busybox wget(`modloop=` · `apkovl=`) · apk(`alpine_repo=`)가 `http://pxe:<secret>@…/assets/…` 를 받아 진단이 완주하는지. 시리얼 콘솔에 `modloop` · `apkovl` 실패가 없어야 한다. firstboot 의 `agent.sh` 수신은 credential 없는 경로(`provision_base`)라 `/assets/agent.sh` 만 대역 제한 아래 열려 있다(CP5 F-1 정정) — 실기에서도 `firstboot` 가 401 없이 에이전트를 띄우는지 시리얼로 확인.
- [ ] **재진입 chain 의 절대 credential URL** (S19-2 Q4): 대기 스크립트(입고 검수 · HOLD · 실패 · 결손)의 `chain http://pxe:<secret>@…/boot?…` 재진입이 30초 주기로 이어지는지 — 상대 경로 시절의 credential 상속에 기대지 않는 것이 요점.
- [ ] **프로비저닝 LAN 제한의 실기 판정** (S19-2 D-3): 스테이징 nginx 뒤에서 `PXE_GUEST_ALLOWED_CIDRS=192.168.1.0/24` 로 실기망 게스트가 통과하고, 사무실 대역에서 게스트 채널을 부르면 403 `PXE_LAN_FORBIDDEN` 인지 — `X-Forwarded-For` 가 출발지로 쓰이는지(`SERVER_FORWARD_HEADERS_STRATEGY=native` · internal-proxies 127.0.0.1) 앱 로그 · 응답으로 확인.
- [ ] **secret 노출 범위의 실측** (S19-2 D-4 노출 범위): 게스트 `/proc/cmdline` · iPXE 콘솔 · nginx 접근 로그 · 앱 로그 어디에 secret 이 보이는지 기록 — 앱 로그는 userinfo 를 받지 않는다(브라우저 · iPXE · wget 은 Authorization 헤더로 보낸다).

## 완료 기록

- **2026-09-18 — HF20 부트 순서 정착 실기 K-1 · K-2 · K-3** (계획 = `plan/26-09-17_16-47-00_HF20_plan.html` §4 · 결과 = HF20 report, 원장 = Notion `HF20`). 2호기(MS04-CE0 · BMC 10.1.1.9) · 스테이징 17차 → 18차. K-1(정의서 1) 통과 · K-2(정의서 9) Windows Setup 첫 재부팅 셸 낙하(G-1 · Once=Hdd 보완) · K-3(정의서 13 · 18차) 손대지 않고 완주. 확정 거동 = 맞바꿈 규칙 · 항목 집합이 바뀌는 POST 의 목록 재구성(셸 → 디스크 → NIC) · Once 는 다음 POST 에 직전 순서 복원 · BootOrder PATCH 는 유지 · BMC FBO 속성은 지연 지표.
- **2026-09-08 — Windows Server 2025 앱 통합 실기 2호 Run 3(다중 디스크)** (계획 = `discussion/26-09-04_08-48-13_E4-1-a-5_windows-fieldwork-2_checklist.md` §6-2 · 결과 §9-2, 원장 = Notion `E4-1-a-5`, 장비 = MS04-CE0 · 9361-8i · RAID1 446 GB `spvR1V1` + RAID5 18 TB(앱 밖 생성) · VM dev 3448d57). **D1 O · D4 O · D2 부분(UniqueId = WWN)** — 진단 lsblk(sda 446.6G → sdb 18.2T)와 WinPE diskpart(디스크 0 446 GB → 디스크 1 18 TB · 2~5 = BMC 가상 미디어 0 B)의 열거가 일치하고, `WillWipeDisk=true · DiskID=0` 은 디스크 0 만 지워 18 TB 볼륨의 표식 파일이 살아남았으며 C: 는 RAID1 에 들어갔다. 무인 완주 12분 54초 · 재진입 0 · 완료 보고 200 · 종단. D2 는 같은 모델 물리 디스크 직결이 없어 부분 — 대신 Windows `Get-Disk` UniqueId 가 RAID 볼륨 WWN 과 두 디스크 모두 정확히 일치함을 확인(물리 디스크는 JBOD 서버에서 재실측). 게스트 로그 재계산 = 게시 드라이버 47 · 문제 장치 0. 파생 F-R3-1 = 완료 보고 '드라이버 0' 오보 — VM `$OEM$` 이 09-04 조립본(HF11-1 이전 스크립트)이라 생긴 표시 오류(원장 로그 꼬리는 드라이버 설치 · 문제 장치 0) → 배포 뒤 [조립] 재실행. Q3 입력 = 서버 사전 계산 전제 1회 성립 · 계약은 WinPE 검증(크기 · WWN) 동반으로 → E4-1-a-6 CP1.
- **2026-09-04 — Windows Server 2025 앱 통합 실기 2호 Run 1** (계획 = `discussion/26-09-04_08-48-13_E4-1-a-5_windows-fieldwork-2_checklist.md`, 원장 = Notion `E4-1-a-5`, 장비 = MS04-CE0 · 시리얼 PGC87000014 · 9361-8i RAID1 · BIOS F29). **W1~W6 전부 통과** — 정의서 할당 · 개시 → 토큰 번들 wimboot 체인 → 무인 완주(12분 37초 · 개입 0) → 앱 조립 `$OEM$` 의 SetupComplete(드라이버 47 · 문제 장치 0) → 첫 로그온 완료 보고 200 · 종단 · 토큰 회수 → 재PXE `exit` 로 로컬 Windows. 미수행: K1(재진입 — 디스크 우선 부트라 재PXE 없음) · K2(스윕) · D1 · D2 · D4(다중 디스크) · S(Secure Boot). 파생 결함 3 → **HF11 묶음**(HF11-1 · HF11-2 · HF11-3, 2026-09-04 신설): **F-W1** 보고 스크립트가 pnputil 영문 출력만 파싱(ko-KR → 드라이버 0 오보; 정정안 = `oemNN.inf` 개수 · `Get-PnpDevice` Status) · **F-R13** 수집 완료 게스트의 진단 재진입 시 재수집 없음 + 개시 소급 전진 조건 불충족(회수 · 재등록으로 우회) · **F-OEM** 손조립 `$OEM$` 자식 디렉토리 소유권으로 첫 조립 500(사전 판정 · `.old` 정리). 운영 함정 2(런북 반영): EnvironmentFile 백슬래시 · 유닛 ProtectSystem drop-in. 관찰: BMC 가상 미디어가 Windows 디스크 1~4 로 열거(크기 0 · USBSTOR — E4-1-a-6 입력).
- **2026-08-19 — BMC Redfish 실측 4호 세션 J1** (계획 = `discussion/26-08-19_13-59-00_bmc-redfish-fieldwork-4_briefing.md`, 원장 = Notion `E0-4-4`). **fat32-lib 조립 이미지의 실기 부팅 왕복 통과** — PXE 서버 잠정 사용 중단 · BMC 가상 미디어 실행 불가로 전달 통로를 물리 USB(dd)로 변경해 수행. 실기 UEFI(AMI Aptio V)가 fat32-lib FAT32 super-floppy(100 MB · edk2-stable202002 Shell)를 인식하고 `startup.nsh` 의 fs 번호 비의존(마커 `spv-fw.tag` 순회) 탐지가 동작함 — **E2-2 의 이미지 동적 생성 = fat32-lib 인프로세스로 확정, mtools fallback 불요**. 잔여 = "fat32-lib 이미지 × sanboot 전달" 조합(위 E2 절 적립 항목). **파생 관찰: OS 설치 서버는 boot priority 가 OS 1순위** — BootSourceOverride 승격 검토의 계기(위 강화 확장 절, 4호 K1 신설).
- **2026-08-19 — BMC Redfish 실측 3호 세션** (계획 = `discussion/26-08-19_11-15-57_bmc-redfish-fieldwork-3_briefing.md`, 원장 = Notion `E0-4-3`). **BIOS 설정 Redfish 쓰기 전 왕복 실증**(위 E3 항목 — E3-1 전제 복구). **핵심 파생 발견: F27 → F29 flash 가 전원 계열 설정을 기본값으로 초기화함** — F27 시점 값(SpeedStep Disable · PackageCState C0/C1 · BIOS Controls EPB 등)이 E0-1 의 사내 표준 세팅과 일치했는데 F29 후 전부 기본값(Enable · Auto · OS Controls EPB)으로 바뀜. 즉 "펌웨어 업데이트 → 설정 소실 → 재적용 필요" 가 실측됐고, `ProvisioningPhase` 의 FIRMWARE_UPDATING → FIRMWARE_SETTING 순서 설계가 실증적 근거를 얻음. 또한 "표준 이미지 flash + Redfish 설정 재적용" 파이프라인이 사내 커스텀 이미지 요구의 대체 경로로 성립(벤더 문의 ① · ② 의 중요도 하락). `Systems/Self/SD` = ComputerSystem(Boot 등) pending 으로 별개 확인. Syslog 는 `NetworkProtocol` 응답에 부재 확정(Redfish 트리 밖 — E3-R 추정 확정). H4 는 캡처 화면이 어긋나(SEL 이벤트 조회 `/api/logs/event`) Syslog 설정 화면 재캡처 이월. 잔여: I1(PFR 버전) · I2(ipmitool) · I3(전원 왕복 관측) · H4 재수행.
- **2026-08-19 — BMC Redfish 실측 2호 세션** (계획 = `discussion/26-08-19_09-29-05_bmc-redfish-fieldwork-2_briefing.md`, 원장 = Notion `E0-4`). **BIOS 의 Redfish flash 실증**(표준 이미지 한정 — 커스텀 이미지 미결로 E2-2 설계 분기는 벤더 문의 후 확정), 전원 제어 완결(위 항목), 보드 시리얼 `QG260700082` 채집(Chassis Oem — E3-0 입력). **부정 실측 2건**: `@Redfish.Settings` 링크 부재 + `GET /redfish/v1/Systems/Self/Bios/SD` 404 — **공식 가이드의 BIOS 설정 PATCH 경로(`Bios/SD`)가 이 펌웨어에 없다**(E3-1 전제 재검 필요 — 1호에서 실측된 `Systems/Self/SD` 가 대체 후보, 미확인) / 세션 발급 415 는 Content-Type 헤더 누락이 원인(경로 자체는 실재 — 재시도 항목). 잔여: 커스텀 이미지 벤더 문의 · `Systems/Self/SD` 확인 · `HPM_BIOS2` 용도 · Syslog 위치(F3 이월) · 복구 절차(F4 이월).
- **2026-08-18 — BMC Redfish 실측 세션** (계획 = `discussion/26-08-15_15-48-41_bmc-redfish-fieldwork-checklist_briefing.md`, 원장 = Notion `E0-4 : BMC Redfish API 경로 확인 작업`, 장비 = MS04-CE0 · BIOS F27 · BMC 13.06.26 → 13.06.27). E2-3 착수 게이트 ①(OEM 계약 — 모양 갱신 확인) · ②(전원 OFF 수락 — 실집행 완주) 통과, ForceOff 실증. 부수 수확: credential증명 PATCH(fresh ETag + If-Match) 204 실증(E3-0 부트스트랩 경로 확정) · BIOS `Attributes` 키 체계 전량 채집(`BirchStream` · `GBT` · `NWSK` · `SETUP` · `TCG` 접두 — E3-1 · BIOS 세팅 템플릿의 실측 입력) · `BootSourceOverrideTarget` 허용값에 `Pxe` 실측(UC-4 원격 복구 재료) · 듀얼 이미지 실물 확인(Image1 13.06.26 / Image2 13.06.24). **잔여 5건**: Reset `On` 재투입 · Redfish 세션 발급 경로(B1 — POST Sessions 불명, Basic auth 는 가용) · `SimpleUpdateActionInfo` 내용(A4) · Syslog 설정 위치(C4) · 실패 복구 절차(A7).

### E4 — OS 설치
- [~] **재PXE 실측 K1** (E4-1-a-3) — **2026-09-04 부분**: 부트 순서가 디스크 우선이라 Setup 재부팅이 재PXE 로 돌아오지 않음(재진입 0) · 종단 뒤 수동 네트워크 부팅은 `exit` 폴스루로 로컬 Windows 부팅 확인. 네트워크 우선 회차(Run 2)가 남음: Windows Setup 의 재부팅이 부트 순서(네트워크 우선 · 디스크 우선)에 따라 재PXE 로 돌아오는지 — 돌아오면 `exit` 폴스루로 로컬 디스크 부팅이 이어지는지(재진입 n/5 콘솔 문구 · 카드 재진입 수 대조), 돌아오지 않으면 시한 판정이 일어나지 않는 공백(D-8)을 운영자 수동 실패 전환으로 닫는 절차 확인.
- [x] **토큰 번들 wimboot 체인 실기** (E4-1-a-3) — **2026-09-04 통과**(실측 2호 Run 1: 번들 5 파일 200 · boot.wim 542 MB 6.5초 · WinPE 부팅). Range 재수신은 미측정: 실 iPXE 가 `kernel …/windows/{token}/wimboot` + `initrd` 4(winpeshl.ini · install.bat · autounattend.xml · boot.wim 542 MB) 를 HTTP 로 받아 WinPE 로 부팅하는지 · boot.wim 전송 중단 시 Range 재수신 여부.
- [x] **렌더본으로 무인 설치 완주** (E4-1-a-3) — **2026-09-04 통과**(서빙 → 완료 보고 12분 37초 · 개입 0 · `hostname` = SPV-0F961A9D): 앱이 렌더한 autounattend.xml(언어 = 이미지 태그 · ComputerName `SPV-<UUID 8>` · Base64 비밀번호 · 제품 키)과 install.bat(UNC · 계정)로 실측 3호와 같은 무인 완주 · 첫 로그온 · `hostname` = ComputerName.
- [x] **$OEM$ 페이로드 · SetupComplete 실기** (E4-1-a-4) — **2026-09-04 통과**(앱 조립본 · SetupComplete 실행 · 게시 드라이버 47 · QAT DEV_4944 ×2 · 문제 장치 0 · GVLK 에서 실행됨): 앱이 조립한 `sources/$OEM$`(드라이버 트리 · SetupComplete.cmd · spv-report.ps1)를 Setup 이 복사하고, 첫 로그온 전에 SetupComplete.cmd 가 pnputil 루프를 실제로 실행하는지(`C:\SPV\setupcomplete.log` · 문제 장치 92 → 0 재현) · 제품 키 종류(GVLK)에서 건너뛰지 않는지.
- [x] **첫 로그온 완료 보고 도달** (E4-1-a-4) — **2026-09-04 통과**(15:13:28 HTTP 200 closed · 종단 · 토큰 404 · 카드 완료 · 재부팅 `exit`). 파생 결함 F-W1 = 보고 스크립트의 pnputil 영문 파싱(ko-KR 에서 드라이버 0 오보) → HF: FirstLogonCommands Order 2 의 `spv-report.ps1` 이 네트워크 대기 뒤 `POST /api/pxe/v1/agent/windows/complete` 로 200 을 받고(`C:\SPV\spv-report.log`), 카드가 완료(ComputerName · 드라이버 n · 문제 장치 n)로 바뀌며 토큰 URL 이 401(S19-1 게스트 체인 · 종전 404)이 되는지 · 종단 뒤 재부팅이 로컬 디스크로 이어지는지(exit).
- [ ] **재PXE 없는 게스트의 스윕** (E4-1-a-4): 디스크 우선 부트 순서에서 보고가 오지 않을 때 서빙 + 60분 + 유예 30분 뒤 스윕이 INSTALL_TIMEOUT 으로 닫는지(운영자 수동 전환 없이).
- [ ] **wimboot 서명 · Secure Boot** (E4-1-a-3 · E4-1-a-5): 2011 CA 서명 wimboot 를 실보드 펌웨어가 Secure Boot 켠 상태에서 받는지(2026-06 CA 전환 뒤 거부 가능 — 런북 §14-4).
- [ ] **서빙 시점 드라이버 선택 목록의 실행** (R15-2 · 2026-09-13 적립): ① specialize 패스의 `RunSynchronousCommand`(`cmd /c mkdir C:\SPV & curl.exe -fsS -o C:\SPV\spv-drivers.lst "<토큰 URL>/spv-drivers.lst"`)가 실기에서 목록을 받는지 — 그 시점에 네트워크가 올라와 있어야 하며, NIC 드라이버가 inbox 에 없는 보드면 실패해 `:legacy`(전체 INF 루프) 로 내려간다. 판별은 `C:\SPV\setupcomplete.log` 의 `[SPV-INSTALL] -|LEGACY|exit=0` 유무. ② SetupComplete 가 목록대로 분기하는지 — ASPEED `MSI|4_aspeed-driver|WDDM Installer\Win2025.msi|-|1` 이 `msiexec /i … /qn /norestart` 로 설치되고(종료 코드 0 또는 3010) `TREE` 행은 pnputil 폴더 전체, `INF` 행은 파일 단위. ③ ~~재부팅 플래그 1 이 있으면 끝에 `shutdown /r /t 10` 이 걸리고, 재부팅 뒤 첫 로그온의~~ **→ 2026-09-16 실기(HF17 회차)에서 결함으로 확정 — 자동 로그온은 1회(LogonCount=1)라 SetupComplete 의 재부팅이 첫 로그온의 보고 스크립트를 끊어 완료 보고가 유실되고 종단이 안 됐다. HF18 정정 = SetupComplete 는 `C:\SPV\reboot-required` 플래그만 남기고, `spv-report.ps1` 이 보고 전송(성공 또는 재시도 소진) 뒤 `shutdown.exe /r /t 5`. 확인 = 보고 도착 → 종단 → 그 뒤 재부팅 1회. 배포 시 스크립트 해시가 바뀌므로 대시보드 "드라이버 페이로드 조립" 을 다시 눌러 `$OEM$` 을 갱신해야 한다.** 그 다음 `spv-report.ps1` 이 `[SPV-INSTALL]` 줄을 `installs` 로 실어 카드의 "설치 결과 (n)" 접이식에 폴더 · 모드 · 종료 코드가 보이는지. ④ 빈 필드 자리표시자 `-` 가 `for /f "tokens=1-5 delims=|"` 에서 다섯 필드를 그대로 유지하는지(샌드박스는 렌더 텍스트로만 확인 — CP5 F-0 정정의 실기 확증).
