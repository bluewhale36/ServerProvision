# ServerProvision 디자인 시스템

관리 UI 의 시각 규칙을 정한다. 규칙의 구현은 `src/main/resources/static/global/style.css` 이고, 화면 하나에만 뜻이 있는 규칙은 그 화면의 CSS 파일과 템플릿 클래스가 가진다. 이 문서와 코드가 어긋나면 코드를 고치거나 이 문서를 고친다. 둘을 다르게 두지 않는다.

## 1. 원칙

이 도구가 다루는 것은 문서가 아니라 장비 대장이다. 화면 대부분이 식별자, 주소, 버전, 해시로 채워진다. 그래서 종이 은유(따뜻한 회색, 둥근 카드, 부드러운 그림자)를 쓰지 않고 다음 원칙으로 계층을 만든다.

1. **계층은 위치와 격자가 진다.** 왼쪽 사이드바가 지금 어느 영역에 있는지 말하고, 판(카드) 안의 격자선이 칸을 말한다. 면의 크기나 그림자로 계층을 만들지 않는다.
2. **같은 뜻은 어느 화면에서든 같은 클래스다.** 비활성은 언제나 `n-badge-off` 이고, 사용 중단 권고는 언제나 `n-badge-yellow` 다. 한 화면의 사정으로 전역 규칙에 예외를 만들지 않는다. 6절의 상태 어휘 표가 기준이다.
3. **표시는 조작보다 작다.** 배지(11px)는 버튼(12px 이상)보다 한 단 작다. 같은 줄에 나란히 설 때 어느 것이 눌리는지 크기로 가른다.
4. **조작의 색은 결과 상태를 말한다.** 활성화 버튼은 활성 배지의 초록을 테두리로 두르고, 비활성화 버튼은 결과에 색이 없으니 중립 회색이다. 채움은 화면의 주 동작(파랑)과 보조 동작(회색) 둘뿐이다.
5. **오류는 한 색이다.** 배너, 입력 테두리, 필드 메시지가 모두 `--n-error` 를 본다. 되돌릴 수 있는 삭제의 주황(주의)과 갈라진다.
6. **화면 특수 규칙은 그 화면에 둔다.** 전역 시트의 선택자를 특정 화면 사정으로 덮어쓰면 다른 화면이 오염된다. 그 화면의 템플릿 클래스와 페이지 CSS 로 해결한다.
7. **지원 폭은 1024px 이상이다.** 데이터센터 도구의 주 사용처는 넓은 모니터이고 1024 는 안전망이다. 그 아래는 지원하지 않는다.

## 2. 토큰

모든 색, 반지름, 글꼴은 `style.css` 의 `:root` 토큰으로만 쓴다. 규칙 본문에 색 값을 직접 적지 않는다. 예외는 형태가 곧 뜻인 원(`border-radius: 50%`)과 어두운 사이드바 위의 글자색뿐이다.

| 토큰 | 값 | 쓰임 |
|---|---|---|
| `--n-bg` | `#eef1f5` | 페이지 바탕 |
| `--n-surface` | `#ffffff` | 판, 입력, 표 본문 |
| `--n-sunken` | `#f7f9fb` | 판 안에서 한 단 꺼진 면. 구획 머리, 묶음 배경, 업로드 패널 |
| `--n-thead` | `#e3e8ef` | 표 머리와 Miller 열 머리 |
| `--n-row-hover` | `#f1f5fa` | 행 위에 마우스를 올렸을 때 |
| `--n-text` | `#0f172a` | 본문 |
| `--n-text-sec` | `#475569` | 보조 글자, 힌트, 표 머리 |
| `--n-text-muted` | `#94a3b8` | 값 없음, 자리 표시, 비활성 링크 |
| `--n-border` | `rgba(15, 23, 42, .14)` | 판 테두리, 구획선 |
| `--n-border-row` | `rgba(15, 23, 42, .08)` | 표의 행 구분선과 세로 격자선 |
| `--n-border-form` | `rgba(15, 23, 42, .22)` | 입력 테두리, 중립 버튼 테두리 |
| `--n-blue` | `#2557d6` | 주 동작, 링크, 활성 표시 |
| `--n-blue-hover` | `#1c44a8` | 주 동작 버튼의 hover |
| `--n-blue-badge-bg`, `--n-blue-badge-text` | `#eaf0fd`, `#2557d6` | 정보 배지 |
| `--n-green` | `#0f8a4d` | 활성, 성공 |
| `--n-yellow`, `--n-yellow-bg` | `#a97a1e`, `#fbf3e2` | 사용 중단 권고, 경고 |
| `--n-orange` | `#c2560a` | 주의. 미설정, 불일치, 되돌릴 수 있는 삭제 |
| `--n-red`, `--n-red-hover` | `#c02626`, `#951c1c` | 오류, 되돌릴 수 없는 삭제 |
| `--n-teal` | `#17868c` | 완료, 연결됨 |
| `--n-warm-dark` | `#16202e` | 사이드바, 어두운 머리 |
| `--n-error` | `var(--n-red)` | 오류 축 전체 |
| `--n-radius` | `4px` | 낱개로 서는 작은 조각. 버튼, 입력, 배지, 코드, 칩 |
| `--n-radius-lg` | `6px` | 내용을 담는 판. 카드, 알림, 모달, 패널, 아코디언 |
| `--n-shadow-card` | `none` | 그림자는 쓰지 않는다. 토큰은 옛 규칙과의 호환으로 남긴다 |
| `--n-font-mono` | SFMono, Menlo, Consolas 계열 | 식별자, 경로, 해시. 앱 전체에서 한 벌 |

반지름은 두 값뿐이다. 낱개로 서는 작은 조각은 4px, 내용을 담는 판은 6px 이다. 모서리를 지우지 않는 이유는 격자가 이미 구조를 지고 있어 각을 세울 필요가 없기 때문이고, 값을 둘로 줄인 이유는 같은 성격의 요소끼리 모서리가 달랐던 것(알림 8, 아코디언 6, 카드 12, 배지 알약)을 없애기 위해서다. 알약(9999px)은 쓰지 않는다.

## 3. 타이포그래피

글꼴은 Inter 이고 숫자는 고정 폭(`font-variant-numeric: tabular-nums`)이다. 표에서 자리수가 흔들리지 않는다. Inter 는 `static/fonts/inter/` 의 가변 폰트 두 파일(latin, latin-ext)을 `style.css` 의 `@font-face` 가 싣는다. 외부 글꼴 서비스를 쓰지 않는다. 한글은 Inter 에 없어 시스템 글꼴이 그리므로, 자간 값은 라틴 글자에만 뜻이 있다. 크기 위계는 다음 표를 넘지 않는다. 새 크기를 만들지 않고 이 안에서 고른다.

| 역할 | 크기 | 굵기 | 클래스 |
|---|---|---|---|
| 페이지 제목 | 26px | 700 | `n-page-title` |
| 상세 절 제목 | 18px | 600 | `n-detail-section > .n-section-title` |
| 판 제목, 묶음 제목 | 15px | 600 | `n-card-title`, `n-glist-title` |
| 본문 | 15px | 400 | body |
| 표 본문, 상세 표 값 | 14px | 400 | `n-table td`, `n-detail-table td` |
| 버튼 | 14px | 600 | `n-btn` |
| 작은 버튼 | 12px | 600 | `n-btn-sm` |
| 사이드바 링크 | 13.5px | 400 (활성 600) | `n-nav-link` |
| 라벨 | 13px | 600 | `n-label` |
| 되돌아가기 | 13px | 500 | `n-back-link` |
| 힌트, 오류 메시지 | 12px | 400 | `n-hint`, `n-error` |
| 배지 | 11px | 600 | `n-badge` |
| 표 머리, 구획 라벨 | 11px 대문자 | 600 | `n-table th`, `n-section` |
| 사이드바 영역 라벨 | 10px 대문자 | 700 | `n-nav-group` |
| 인라인 코드 | 0.85em | 500 | `code` |

크기 위계는 아끼는 축이다. 폼 항목의 라벨은 크기가 아니라 굵기(600)와 색(`--n-text`)으로 힌트와 갈린다.

## 4. 레이아웃

### body 가 곧 shell 이다

모든 화면은 `<body>` 바로 아래에 `<nav class="n-nav">` 와 본문 컨테이너를 형제로 둔다. `body` 가 가로 flex 라 사이드바와 본문이 나란히 서고, 모달 오버레이(`position: fixed`)는 뷰포트 전체를 덮으므로 보정 규칙이 필요 없다. 감싸는 요소를 더하지 않는다. 본문 컨테이너는 `flex: 1 1 auto` 와 `min-width: 0` 을 가져 긴 식별자가 사이드바를 밀어내지 못한다.

본문이 하나의 컨테이너로 시작하지 않는 화면(BIOS 설정 편집기)은 `bios-console` 처럼 그 화면의 래퍼 하나로 세로 쌓임을 맡긴다.

### 사이드바

폭 228px, `position: sticky` 로 뷰포트에 붙는다. 배경은 `--n-warm-dark` 이고 영역 라벨(`n-nav-group`: 자원 관리, 운영, 프로비저닝)이 링크 묶음의 이름이다. 활성 링크는 왼쪽 3px 파란 선과 밝은 배경으로 표시한다. 뷰포트가 낮아 링크가 100vh 를 넘으면 사이드바 안에서 스크롤한다. 맨 아래에 배경 작업 버튼이 서고, 그 패널은 사이드바 오른쪽 아래(`left: 240px; bottom: 16px`)에 뜬다.

### 본문 컨테이너

화면 성격으로 고른다. 안쪽 여백은 세 클래스 모두 `24px 28px` 이다.

| 클래스 | 최대 폭 | 쓰는 곳 |
|---|---|---|
| `n-page` | 1440px | 목록, 대시보드. 표가 화면 폭을 쓰는 화면 |
| `n-page-lg` | 1200px | 표와 다단 카드가 있는 상세, 작성 화면. 세팅 정의서, 게스트 서버 상세 |
| `n-page-md` | 960px | 단일 열 입력 폼, 단순 상세. 관리 자원의 new, edit |

### 반응형

앱의 브레이크포인트는 1024px 하나다(기존 900px 규칙은 그 아래 안전망으로 남아 있다). 1024 이하에서 사이드바는 228px 을 유지하고 본문 안에서 접는다. 반응형 규칙은 그 컴포넌트를 소유한 파일에 둔다. 페이지 CSS 가 `style.css` 뒤에 실리므로 같은 명시도면 전역 시트의 규칙이 지기 때문이다.

| 컴포넌트 | 1024 이하 거동 | 파일 |
|---|---|---|
| Miller | 상세 열이 100% 폭으로 아래에 스택된다. 목록 열은 첫 줄을 나눠 갖고 각자 340px 안에서 스크롤한다 | `css/miller.css` |
| 게스트 서버 표 | 제조사, 보드 모델 열을 숨기고 식별자 열을 넓힌다. UUID 와 등록 시각은 두 줄로 접힌다 | `provisioning/server.css` |
| 탭 스트립 | 안쪽 가로 스크롤 대신 줄바꿈 | `global/style.css` |

## 5. 컴포넌트

### 페이지 머리와 되돌아가기

`n-page-header` 는 왼쪽 `n-page-title`, 오른쪽 `n-header-actions` 로 구성하고 페이지에 하나만 둔다. 제목은 줄어들지 않고(`flex-shrink: 0`) 단어 안에서 끊기지 않는다(`word-break: keep-all`). 한국어는 기본값이 음절 단위 줄바꿈이라 오른쪽 동작 묶음이 길면 제목이 '시스템 자 / 산' 으로 갈라지기 때문이다. 목록으로 돌아가는 링크(`n-back-link`)는 이 머리 안, 제목 오른쪽에 선다. 버튼이 아니라 경로이므로 테두리와 배경이 없고 올리면 파랗게 된다. 제목 옆 배지는 그 상태의 클래스를 그대로 쓴다. 제목 문맥이라고 색을 바꾸는 규칙은 두지 않는다.

클래스가 없는 맨 링크(`<a>`)도 팔레트의 파랑(`--n-blue`)이다. 전역 시트가 이 값을 두지 않으면 브라우저 기본 파랑이 남아 한 화면에 파랑이 둘이 된다.

### 판과 구획

`n-card` 는 흰 면에 `--n-border` 테두리, 반지름 6px, 그림자 없음이다. `overflow` 는 `clip` 이고 `overflow-clip-margin: 90px` 이라 툴팁이 판 밖으로 나가도 잘리지 않는다. `overflow-clip-margin` 은 Safari 가 지원하지 않는다. 관리 UI 는 Chrome 과 Edge 를 전제하므로 그대로 두되, Safari 에서는 툴팁이 판 경계에서 잘린다. 판 안 여백(`n-card-body`)은 `22px 24px` 이다.

`n-section` 은 판 안의 구획 머리다. 꺼진 면(`--n-sunken`) 위에 대문자 11px 라벨이 서고 왼쪽에 3px 파란 선이 있다. 밑줄 하나로 나누던 옛 방식은 쓰지 않는다. `n-card-body` 의 직접 자식으로만 둔다.

`n-card-head` 는 판의 머리다. 제목(`n-card-head-title`, 16px), 집계 배지, 판 단위 동작이 한 줄에 서고 왼쪽 묶음은 `n-card-head-group` 으로 감싼다. 표 머리와 같은 꺼진 면 위에 서고 아래 격자선으로 본문과 갈라지며, `n-card-body` 없이 표를 바로 담는 판(시스템 자산 대시보드의 탭 패널)에도 붙는다. `n-card-title` 은 판 안 여백 속의 구획 제목이고 `n-card-head` 는 판의 테두리에 붙는 머리라는 점이 다르다.

묶음 목록(`n-glist`)은 꺼진 면 위에 흰 판(`n-glist-panel`)을 얹는 3층 구조다. 묶음 머리(`n-glist-head`)는 아래 격자선으로 판과 갈라지고, 그 안의 버튼은 제목보다 커지지 않도록 작은 크기로 고정된다.

### 표

`n-table` 은 격자선을 그린다. 머리는 `--n-thead` 면에 대문자 11px, 본문은 `8px 14px` 여백에 14px 이다. 세로선은 행 구분선과 같은 `--n-border-row` 이고 마지막 칸은 판의 테두리가 대신한다. 행 위에 올리면 `--n-row-hover`.

상세로 가는 길은 행의 ID 가 맡는다. `n-td-id` 안의 `n-row-link` 는 파란 글자에 밑줄을 쉬는 동안에도 두어, 읽기만 하는 값들 사이에서 누를 수 있는 값임이 드러난다. 액션 칸(`n-actions`)에는 상태를 바꾸는 조작만 남기고 "상세" 버튼을 두지 않는다. 행 전체가 상세로 가는 목록(세팅 정의서, BIOS 세팅, 진단 자산)은 `n-row-clickable` 로 손 모양만 더한다. 이 클래스의 정의는 `table-list.css` 한 곳이다. 이동은 행의 `data-href` 를 전역 `row-link.js` 가 문서 위임으로 처리하므로 화면마다 스크립트를 두지 않는다. 셀 안의 링크, 버튼, 입력은 자기 동작을 하고 행 이동을 일으키지 않는다.

셀 안의 값은 잘렸을 때 무엇을 잃는가로 처리를 고른다. 끊을 자리가 없는 식별자(UUID, IP)는 그 열에만 `overflow-wrap: anywhere` 를 걸어 두 줄로 접고, 이름과 시각은 공백에서만 접는다. `anywhere` 를 셀 전체에 걸면 'Gigabyte' 가 'Gigabyt/e' 로 갈라지므로 열 단위로만 건다. 배지 칸은 한 줄이다.

표 안의 배지는 왼쪽 보정을 하지 않는다. 격자선이 칸 경계를 그리므로 배지는 자기 상자로 읽힌다.

`n-table` 과 `n-tbl-list`(폼 안 인라인 편집, `table-list.css` 가 격자와 머리를 스스로 그린다)는 서로 배타적이다. 한 표에 둘을 같이 쓰지 않는다.

### 배지

`n-badge` 는 11px, 굵기 600, `2px 7px` 여백, 반지름 4px 이다. 색이 뜻이므로 새 배지를 만들 때는 6절의 상태 어휘 표에서 클래스를 고른다.

| 클래스 | 모양 | 뜻 |
|---|---|---|
| `n-badge-green` | 초록 채움 | 활성, 성공, 진행 중 |
| `n-badge-off` | 회색 채움 | 비활성. 꺼져 있음이지 주의가 아니다 |
| `n-badge-yellow` | 노랑 채움 | 사용 중단 권고, 경고 |
| `n-badge-orange` | 주황 채움 | 주의. 미설정, 불일치, 정의서 지정(사용 중이라는 정보) |
| `n-badge-red` | 빨강 채움 | 실패, 오류 |
| `n-badge-blue` | 파랑 채움 | 정보, 현재 단계, 등록됨 |
| `n-badge-teal` | 청록 채움 | 완료, 연결됨 |
| `n-badge-gray` | 회색 테두리 | 삭제됨, 상태가 아니라 사실만 알리는 표기 |

Miller 의 상태 태그(`n-miller-status-tag-on`, `-off`, `-warn`)는 같은 값을 쓴다. 사용 중단은 Miller 항목 옆의 노란 점(`n-deprecated-dot`)으로도 표시하며, 점은 형태가 뜻이므로 원(50%)을 유지한다.

### 버튼

`n-btn` 은 14px, `6px 13px` 여백, 반지름 4px 이다. `n-btn-sm` 은 12px, `3px 9px`. 계열은 다음 넷이다.

| 계열 | 클래스 | 모양 | 쓰는 곳 |
|---|---|---|---|
| 주 동작 | `n-btn-primary` | 파랑 채움 | 화면의 한 가지 주 동작. 저장, 등록 |
| 보조 동작 | `n-btn-secondary` | 회색 채움 | 취소, 수정으로 이동, 재검증 |
| 중립 테두리 | `n-btn-outline` | 회색 테두리 | 결과에 색이 없는 조작. 비활성화, 사용 중단 권고 해제, 되돌리기 |
| 결과 상태 테두리 | `n-btn-outline-success`, `-warning`, `-info`, `-danger` | 결과 상태의 색 테두리. 올리면 그 색이 배경으로 | 활성화(초록), Deprecated 표시와 봉인(노랑), 복구와 재시도(파랑), 휴지통 삭제(주황) |
| 되돌릴 수 없음 | `n-btn-danger` | 빨강 채움. 액션 칸 안에서는 진한 빨강 테두리, 올리면 채움 | 영구 삭제 |

상태를 바꾸는 조작은 채우지 않는다. 채우면 배지처럼 보여 표시와 조작이 헷갈린다. 되돌릴 수 없는 것만 예외로 쉬는 동안에도 다르게 보인다. 파랑 채움은 판 하나, 목록 머리 하나에 주 동작 하나뿐이다. 목록 머리의 신규 등록, 폼의 저장, 판의 적용이 그것이고, 두 목록을 가진 화면(드라이버와 유틸리티)이나 판이 여럿인 상세는 판마다 하나씩 가질 수 있다. 상세 안에서 하위 자원을 더하는 버튼(ISO 추가, BIOS 등록)과 Miller 열 하단의 등록 버튼은 보조 회색 채움이다.

크기는 자리가 정한다. 묶음 머리(`n-glist-head`), 상세 표의 값 칸(`n-detail-table .n-actions`), 아코디언 필드(`n-accordion-field .n-actions`) 안의 버튼은 마크업에 `n-btn-sm` 이 있든 없든 작은 크기가 된다. 배지와 나란히 서는 자리이기 때문이다.

버튼 하나를 감싸는 `<form>` 은 `n-actions`, `n-form-actions`, `n-header-actions` 안에서 `display: contents` 가 되어 배치에 끼어들지 않는다. 인라인 스타일로 폼을 세우지 않는다. `n-inline-form` 은 라벨, 입력, 버튼을 한 줄에 세우는 진짜 폼이라 별개다.

비활성 버튼은 계열과 무관하게 같은 중립 회색이다. 비활성 사유는 `n-btn-tooltip-wrap[data-tooltip]` 으로 감싸 올렸을 때 보여 준다. 사용자가 막힐 행동은 서버가 거절하기 전에 이 방식으로 UI 가 먼저 막고 이유를 알린다.

### 폼

항목(`n-form-group`) 사이는 30px 이다. 항목 안의 쌓임(라벨 6px, 힌트 4px)보다 확실히 벌려야 힌트가 다음 라벨과 한 덩어리로 읽히지 않는다. 라벨(`n-label`)은 13px, 굵기 600, 본문색으로 항목의 닻이 된다. 폼 안 항목의 라벨은 `n-label strong`(14px)이다. 힌트(`n-hint`)는 12px 이고 읽기 폭을 68ch 로 제한해 입력에 딸린 주석으로 보이게 한다. 입력 위아래에 힌트를 겹쳐 붙이지 않는다. 주의를 끌어야 하는 힌트는 `n-hint-warn` 으로 색만 바꾼다.

운영 설정 화면(휴지통, 시스템 자산, 자원 무결성 점검)은 항목마다 라벨, 힌트, 메타(`n-form-meta`, 효과 시점 같은 부수 성질), 조작 요소의 순서로 쌓는다. 설명을 읽고 나서 조작하는 순서이고, 세 층의 크기가 14, 12, 11.5 로 내려가 무엇이 항목 이름이고 무엇이 조작 대상인지 갈린다. 세 화면의 폭은 모두 `n-page-md` 다. 같은 뜻의 두 번째 이름(`n-form-label`, `n-form-hint`)은 두지 않는다.

입력(`n-input`, `n-select`, `n-textarea`)은 `--n-border-form` 테두리, 반지름 4px, 포커스 시 파란 테두리와 옅은 파란 고리다. 오류 상태(`is-invalid`, `has-error`)와 오류 메시지(`n-error`, `field-error-message`)는 `--n-error` 한 색이다. 상태 변경 폼과 입력 폼의 마커 규약은 `.claude/domain-conventions/new-form.md` 를 따른다.

### 알림과 배너

`n-alert` 는 반지름 6px 판이다. `n-alert-success`, `n-alert-info`, `n-alert-warning`(막혔거나 되돌릴 수 없는 상태이되 오류는 아닌 것), `n-alert-danger`(오류, `--n-error`), `n-alert-muted`.

`n-banner` 는 페이지 머리 바로 아래의 안내 줄이다. `n-banner-info` 는 도메인 역할 한 문장과 최신 상태 요약(왼쪽 3px 파란 선), `n-banner-warn` 은 다른 도메인의 대기 항목 안내(노랑)다. 이동 동작은 `n-btn n-btn-sm n-btn-secondary`.

### 탭

`n-tabs[role=tablist]` 안에 `n-tab[role=tab][aria-selected][data-panel]`, 패널은 `n-tab-panel` 이고 활성 패널만 `is-active` 로 보인다. 활성 탭은 파란 글자와 2px 밑줄이다. 전환 로직은 `global/n-tab.js` 가 이벤트 위임으로 처리한다. 패널이 표를 담으면 패널 자체에 `n-card` 를 함께 붙여 판 안에 둔다(시스템 자산 대시보드).

### 모달

확인 모달(`cm-*`)과 오류 모달(`gem-*`)의 판은 반지름 6px, 입력과 상태 줄은 4px 이다. 오버레이는 `position: fixed` 라 사이드바와 무관하게 뷰포트 전체를 덮는다. 자원 단위 확인은 서버가 내려주는 모달(`/ui/confirm-modal/{type}`)을, 페이지 단위 동작의 확인은 정적 generic 모달(`ConfirmModal.open()`)을 쓴다. 모달 문구는 S12 문구 규약을 따른다.

### Miller

`n-miller` 는 관리 자원 화면의 3열 선택 패널이다. 열 머리(`n-miller-col-header`)는 표 머리와 같은 `--n-thead` 면이고, 항목 위에 올리면 `--n-row-hover` 다. 정의서 선택 모달이 같은 컴포넌트를 쓰며 `server.css` 는 모달 안이라는 자리의 치수만 덮는다. 1024 이하 거동은 4절을 따른다.

### 인라인 코드와 mono

`code` 는 `--n-font-mono`, 0.85em, 굵기 500, 옅은 회색 면에 반지름 4px 이다. 표의 `n-table-mono`, `n-td-id`, `n-mono`, 모달 입력, 배경 작업 카드의 부제가 모두 같은 글꼴 토큰을 본다. 파일 경로와 MAC 주소가 한 화면에서 다른 글꼴로 보이지 않는다.

여러 줄 코드(요청 본문, Redfish 응답)는 `pre.n-code-block` 이다. 꺼진 면에 테두리와 반지름 6px, mono 12.5px 이고 줄을 접지 않고 가로로 스크롤한다. 화면마다 자기 이름의 pre 를 두지 않는다.

### 콘솔형 화면

BIOS 설정 편집기(`bios-console`)는 펌웨어 화면을 흉내내던 옛 색(짙은 파랑 바탕, 흰 글자, 괄호 선택자)을 버리고 포털의 토큰으로 그린다. 화면 머리의 탭은 `n-tabs` 와 같은 파란 밑줄이고, 항목은 행 구분선으로 나뉘며 입력은 테두리가 있는 mono 다. 오른쪽 도움 패널은 320px 이고 1024 이하에서는 항목 아래로 내려온다. 화면이 콘솔이라는 사실은 배치(항목 표, 키 안내 발)로 말하고 색으로 말하지 않는다. 클래스 이름(`bios-*`)은 편집기 스크립트가 참조하므로 그대로 둔다.

## 6. 상태 어휘와 클래스

같은 상태는 어느 화면에서든 같은 말과 같은 클래스로 표시한다. 표시(배지)와 조작(버튼)이 짝을 이룬다.

| 상태 | 배지 문구 | 배지 클래스 | 그 상태로 만드는 버튼 |
|---|---|---|---|
| 활성 | 활성 | `n-badge-green` | 활성화: `n-btn-outline-success` |
| 비활성 | 비활성 | `n-badge-off` | 비활성화: `n-btn-outline` |
| 사용 중단 권고 | 사용 중단 | `n-badge-yellow` | Deprecated 표시: `n-btn-outline-warning`, 해제는 `n-btn-outline` (조각 `fragments/ui/lifecycle-actions`) |
| 휴지통 | 삭제됨 | `n-badge-gray` | 삭제: `n-btn-outline-danger`, 복구: `n-btn-outline-info` |
| 영구 삭제 | 없음 | 없음 | 영구 삭제: `n-btn-danger` |
| 주의 | 미설정, 불일치, 정의서 지정 | `n-badge-orange` | 없음 |
| 실패 | 실패 | `n-badge-red` | 없음 |
| 정보 | 등록됨, 현재 단계 | `n-badge-blue` | 없음 |
| 완료 | 완료, 연결됨 | `n-badge-teal` | 없음 |

문구는 S16 어휘 표준을 따른다. 배지 문구와 버튼 문구는 다르다. 배지는 상태(활성), 버튼은 동작(활성화)이다.

## 7. CSS 작성 규칙

- 외부 자원을 싣지 않는다. 시트, 스크립트, 글꼴은 전부 저장소 안에 있다. 워커의 `importScripts` 도 같다. 타사 라이브러리는 `static/vendor/<이름>/` 에 라이선스 파일과 함께 둔다(`hash-wasm`). 브라우저 기본값 정돈은 `global/reset.css` 가 맡고 `style.css` 보다 먼저 실린다. 이 파일은 종전 Bootstrap 이 실제로 주던 39 규칙을 값 그대로 옮긴 것이라, 여기에 새 규칙을 더하지 않는다. `[hidden]` 을 `display: none !important` 로 두는 것이 이 파일의 가장 중요한 한 줄이다. 배너와 모달은 클래스가 `display` 를 세우고 `hidden` 속성으로 숨기기 때문이다.
- 색과 반지름은 토큰만 쓴다. 규칙 본문에 색 값이나 px 반지름을 적지 않는다.
- 인라인 스타일을 쓰지 않는다. 템플릿의 `style=` 은 0 이다. 간격과 부연은 역할 헬퍼로 표현한다. `n-block-gap`(앞 블록과 띄움), `n-block-gap-sm`, `n-lead`(판을 여는 안내 문단), `n-flush`, `n-stack`(판 안에서 잇달아 쌓이는 블록), `n-note`(값 옆 부연), `n-meta`(배지 뒤 부연), `n-small`, `n-strong`, `n-btn-block`, `n-field-row`(입력 옆 버튼), `n-check`와 `n-check-row`, `n-break`. 값 이름(`mt-16`)으로 된 헬퍼는 만들지 않는다.
- 전역 시트(`style.css`)는 두 화면 이상이 같은 뜻으로 쓰는 규칙만 가진다. 한 화면의 사정은 그 화면의 CSS 파일과 템플릿 클래스에 둔다. 전역 선택자를 특정 화면 문맥으로 덮어쓰는 규칙(`.n-page-title .n-badge-gray { ... }` 같은 것)은 만들지 않는다.
- 페이지 CSS 는 `style.css` 뒤에 실린다. 같은 명시도면 뒤에 실린 쪽이 이기므로, 페이지 CSS 가 전역 규칙을 덮으려면 그대로 쓰면 되고, 전역 규칙이 페이지 규칙을 덮어야 한다면 그 규칙은 전역이 아니라 페이지 파일의 것이다.
- 변형 규칙은 기본 클래스와 겹쳐 쓴다(`.cm-card.cm-card-wide`). 명시도가 같으면 적재 순서가 승부를 가르고, 그 원인은 마크업 어디에도 보이지 않는다.
- 반응형 규칙은 컴포넌트를 소유한 파일에 둔다. 브레이크포인트는 1024 하나다.
- 새 상태를 표시할 때 새 색을 만들지 않는다. 6절의 어휘에 없는 상태라면 어휘를 먼저 늘리고 토큰을 정한다.
- 클래스 이름은 `n-` 접두를 유지한다. 접두 자체에 뜻은 없지만 전역과 페이지 클래스가 섞이는 것을 막는다. 이름은 모양이 아니라 뜻으로 짓는다(`n-badge-off` 이지 `n-badge-lightgray` 가 아니다).

## 8. CSS 클래스 사용 제약

이름만으로 용도가 자명한 클래스는 생략하고, 오용 가능성이 있는 클래스만 적는다.

### 데이터 의미 셀 클래스

`n-td-id` 는 레코드의 기본 키나 고유 식별자를 표시하는 셀에만 쓴다. 가운데 정렬, mono, 13px, muted 색이며 안의 `n-row-link` 가 상세로 가는 링크다. 마운트포인트 같은 코드값에는 쓰지 않는다. 코드값(MAC 주소, 경로, 버전)에는 `n-table-mono` 를 쓴다. 이 클래스는 색과 정렬을 바꾸지 않는다.

`n-table-muted` 는 값이 없을 때의 대체 텍스트에만 쓴다. 실제 데이터가 있는 셀에는 쓰지 않는다. `n-table-empty` 는 행이 0개일 때의 안내 행 하나에만 쓰고, `<td>` 에 머리 수와 같은 `colspan` 을 준다.

### 표 타입 선택

| 클래스 | 기반 | 사용 맥락 |
|---|---|---|
| `n-table` | `style.css` | 읽기 전용 목록. 대시보드, 목록 화면 |
| `n-tbl-list` | 자체(`table-list.css`) | 폼 안 인라인 편집. 행에 input, select, 삭제 버튼이 있는 맥락 |

한 표에 둘을 같이 쓰지 않는다. `n-tbl-list` 는 다른 표 클래스 없이 혼자 쓴다.

### 문맥 스타일 오버라이드

`n-detail-fields` 는 읽기 전용 상세 화면의 필드 그룹 래퍼다. 안의 `n-label:not(.strong)` 을 11px 대문자 muted 로 바꾼다. 편집 폼의 폼 그룹을 이 클래스로 감싸면 라벨이 너무 작아진다.

`n-detail-table` 은 2열 key-value 상세 표다. 1열 `<th>` 가 필드명, 2열 `<td>` 가 값이며 `<th>` 폭이 180px 로 고정된다. 다건 목록 표에는 쓰지 않는다. 값 칸 안의 버튼은 배지에 맞춰 작은 크기가 된다.

### 페이지 스코프 컴포넌트

`n-miller-*` 는 `css/miller.css` 에 정의된 Miller 컴포넌트다. 개별 클래스를 떼어 다른 화면에서 쓰지 않는다. 다단 선택 UI 가 필요하면 컴포넌트 구조 전체를 재사용하고, 자리에 맞는 치수만 그 화면의 CSS 에서 겹쳐 쓴다.

OS 관리 화면의 업로드 진행 표시(`n-upload-*`)는 `management/os/os-page.css` 에 있다. 다른 화면에서 쓰려면 먼저 전역 시트로 옮긴다.

### 화면 조각

같은 행이 여러 화면에 나타나면 Thymeleaf 조각으로 둔다. 조각은 `templates/fragments/ui/` 에 있다.

| 조각 | 무엇 | 쓰는 곳 |
|---|---|---|
| `lifecycle-actions :: statusActions` | 활성 배지 + 토글 버튼(부모 차단 툴팁, PRG 선택 복원 입력 포함) | 자원 상세 6, ISO, 세팅 정의서 상세 |
| `lifecycle-actions :: deprecateActions` | 사용 중단 배지 + 표시와 해제(확인 모달 속성 포함) | 같음 |
| `empty :: state(title, desc)` | 판 안 가운데의 빈 상태 | 목록이 빈 화면. 설명에 링크가 필요하면 같은 마크업을 직접 쓴다 |

래퍼(`<tr><th>` 또는 `<dt><dd>`)는 호출하는 템플릿이 가진다. 조각은 그 안의 `n-actions` 만 그린다.

빈 상태는 한 형태다. 판(`n-card`) 안 가운데에 제목 한 줄과 설명 한 줄이다. 판 없이 배경 위에 문단을 두지 않는다. 판 안의 작은 "데이터 없음" 한 줄은 `n-detail-empty` 로 뜻이 다르다. Miller 열 하단의 안내는 `n-miller-col-note` 다.

### 시맨틱 레이아웃 컨테이너

`n-section` 은 `n-card-body` 의 직접 자식으로만 둔다. `n-row` 안에 중첩하거나 범용 제목으로 쓰지 않는다.

`n-form-actions` 는 폼 카드 하단의 최종 동작 줄이다. 폼당 하나이며 위 구분선과 여백을 포함한다. 폼 중간의 인라인 동작에는 `n-actions` 를 쓴다.

`n-page-header` 는 페이지 최상단의 제목과 동작 영역이다. 페이지당 하나, 메인 카드 앞에 둔다.
