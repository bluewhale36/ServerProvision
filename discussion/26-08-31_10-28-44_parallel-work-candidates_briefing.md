# 병행 작업 후보 정리 — 다른 세션에 무엇을 맡길까 (2026-08-31)

RAID 계열(E3.5, 이 앵커 세션이 실측 후 E3.5-1 진행)을 제외하고, 지금 열려 있는 작업을 스트림별로 정리한다. 근거는 Notion 후속 배치(08-28) · 실기 결정 배정 · 각 메모리 · 오늘 확인한 PR/브랜치 상태다.

## 0. 지금 확인된 사실

- 열린 PR 은 **#56 E2.5(network boot 강제)** 하나 — ue 스트림 세션이 진행 중(오늘 자 샌드박스 e25 CP5 기록). **E2.5 는 후보에서 제외**(이미 돌고 있다).
- dev 미병합 원격 브랜치는 E2.5 뿐. 나머지 실기 파생(#49~#55)은 전부 dev 에 병합됨.

## 1. ue 스트림 (실행 엔진 · U 계열) — E2.5 다음 후보

우선순위 순. 전부 Notion 페이지가 있고 `시작 전` 이다.

| 순위 | 코드 | 내용 | 왜 지금 | 의존 |
|---|---|---|---|---|
| 1 | **E2-4** 집행 현황 상세화 | decisions 2(자원이름 · 버전 표기) · 3(flash 시작 SSE) · 6(BMC IP 순서) · 8('끊어짐' 표기) + O-1(침묵 대기 사유) · O-3(실패 사유 표시) | 실기에서 운영자가 화면만 보고 상황을 못 읽던 항목들 — 다음 실기(CP7 일괄) 전에 있으면 실기 자체가 쉬워진다 | 없음. E3.5 와 파일 겹침 적음 |
| 2 | **U3-6** 개시 · 재할당 가드 정합 | decisions 4(할당 없이 개시 → 종단 · 문구 오류 → UI 1차 차단 + 가드 SSOT) + O-4(재할당 문구 불일치) | 실기에서 실제로 밟은 함정. 범위가 작고 독립적 | 없음 |
| 3 | **U6-1** 회수 서버 이름 활성 한정 UNIQUE | decisions 7(`active_name` 생성 컬럼, U6 D-2 수법) | DDL 1건 + 소규모. 재투입 흐름 완성 | 실 DB DDL 적용은 사용자 |
| 4 | **U2-8** BIOS 템플릿 편집기 안내 보강 | O-2(빈 위젯 힌트) · O-6(기본값 복귀 안내) · 400 배너 문구 | 화면 전용 — 엔진과 완전 분리 | 없음 |
| 5 | **U3-7** 서버 목록 시간 그룹 안정화 | decisions 5(`RegistrationAge` 상대 눈금 flicker — 배치 묶기 vs 절대 경계 비교) | 설계 비교가 plan 의 중심이라 다른 슬라이스와 성격이 다름 | 없음 |

참고: **S7-1(SSE)** 은 08-28 dev 병합 완료라 U3-7 등 목록 화면 작업과 충돌 없음.

## 2. 앵커 스트림 (dev 상주 · HF/CH 산발) — 이 세션 또는 짧은 병행 세션

| 분류 | 항목 | 근거 · 규모 |
|---|---|---|
| HF 후보 | **S7-1 후속 O-1** — `/provisioning/server/stream` 과 `/{id}` 매핑 충돌(문서 이동 시 400). 경로 이동 또는 `{id}` UUID 정규식 제약 | S7-1 CP5 관찰. 화면 증거 확보됨(B2.png). 소규모 |
| HF 후보 | **S7-1 후속 O-2** — 문서 이동 요청에서 `ApiExceptionHandler` 가 스스로 실패해 WARN 스택 누적(S10 이음매: `IllegalArgumentException` 계열을 `WebExceptionHandler` 가 안 받음) | 요청마다 로그 오염. S10 연장선이라 앵커 소관 |
| HF 후보 | **S7-1 후속 O-3** — 오류 페이지 nav 활성 표시가 엉뚱한 메뉴로 | 표시 문제, 소규모 |
| HF | **시각 의존 테스트 2건 Clock 주입** — DriftBulkApply(항상 실패) · GuestServerQueryServiceGrouping(간헐) | 메모리 적립분. 전체 테스트 신뢰도 직결 — 병행 작업이 늘수록 가치가 커진다 |
| HF | **flashMessage 비동기 제출 소실** — S10 전역 가로채기의 fetch 가 리다이렉트 따라가며 flash 소비(자산 화면 3곳) | 08-13 실측 적립분 |
| HF 보류 | 모달 옵션 버튼 텍스트 넘침(`.n-btn`) — 사용처 전수 조사 후 변형 class | 조사 동반이라 보류 유지 |
| CH | **plan/report 명세 · 에이전트 정의 드리프트** — `.chip.pass/.skip` 미등재 · report 에이전트 정의가 폐지된 임베드본 규약 지시 | S7-1 CP6 에서도 재확인됨. 문서 정비만 |
| S 후보 | 전역 미정의 CSS 클래스 2종(`n-form-banner` 23곳 · `n-btn-outline` 29곳) | 별도 S 단계로 분리돼 있음 |

## 3. 별도 홈 (저장소 밖) — 시기가 맞물린 것

| 항목 | 홈 | 왜 지금 |
|---|---|---|
| **8월 월간 보고서** | `documents/Monthly-Reports`(전용 CLAUDE.md) | 오늘이 8/31 — 작성 시점이 도래. 8월은 실기 통합 테스트 · E3 계열 완주 · RAID 착수가 있어 재료가 많다 |
| **2026-09 세미나 준비** | iCloud `Uniwide/Seminars`(부트스트랩 완료) | 9월이 시작됨 — 발표 일정 확인 후 착수 판단 |

## 4. 사용자 단독 (세션 배정 아님, 전제 조건)

- **E3.5 실측** — 별도 체크리스트(오늘 전달). E3.5-1 의 착수 조건.
- **CP7 실기 일괄** — E1.5 · E2-2 · E3-1 · E3-2 · E3-3 · HF10 · (E2.5 병합 후) + **S7-1 브라우저 확인**. 전제: 실 DB DDL 미적용분(E3-3 스냅샷 표 · U4-1-1 5문 · U6 · MA7 이관분) 적용 + VM 재배포(dev 최신).
- **DPL-2 실배포** — CP7 일괄 통과 후.

## 5. 조건 미충족 · 보류 (지금 배정하지 않는 것)

| 항목 | 보류 사유 |
|---|---|
| R 전수 감사(reconciliation) | 시점 조건이 MK4-2 · MK4-4 뒤로 개정돼 있고 MK4-4-2 가 정지 상태 — 조건 재확인 필요 |
| Windows Server 2025 PXE | 실측 브리핑 준비 단계 — U1 판가름 후 구현 |
| S16 어휘 표준 | S16-1 진단(Opus 위임) 상태 재확인 필요, S16-2 는 디자인 실험 승격과 합류 예정 |
| nav 가로 오버플로 · 반응형 | 반응형 도입 단계 소관으로 적립됨 |
| 디자인 실험 승격 | refine/design-experiment 로컬 브랜치 — 채택분 cherry-pick 시점 미정 |

## 6. 배치 제안 (요약)

- **ue 세션**: E2.5 마무리 → **E2-4** → U3-6 → U6-1 (U2-8 · U3-7 은 그 뒤 취향대로)
- **앵커(이 세션)**: E3.5 실측 접수 · E3.5-1 CP1 이 본류. 짬에 **Clock 주입 HF + S7-1 후속 O-1~O-3** 을 HF 브랜치 하나로 처리 가능
- **별도 세션 1개**: 8월 월간 보고서 (홈이 달라 충돌 없음)
- **사용자**: 실측 → 실 DB DDL + VM 재배포 → CP7 실기 일괄
